# Memory: 2026-04-06

## Session: CRITICAL BUG FIX - check_strategies NOT CALLED

### The Bug
**`check_strategies()` was NEVER being invoked!**

Despite the log saying "✅ TRIGGERING check_strategies", the function was never called. The compiler even warned us: `function check_strategies is never used`.

### Root Cause
```rust
if price.yes_ask > 0 && price.no_ask > 0 { 
    eprintln!("✅ TRIGGERING check_strategies for {}", market.slug);
    // ... but check_strategies() NEVER CALLED!
}
```

The previous implementation logged "TRIGGERING" but never actually invoked the function!

### Secondary Issue: Placeholder Prices
Polymarket sends extreme placeholder prices in initial WebSocket dump:
- YES: bid=1¢, ask=99¢ (98% spread!)
- NO: bid=7¢, ask=8¢ (real prices)

The code was rejecting these as "resolved markets" because `yes_ask >= 98`.

### Fixes Applied

1. **Actually call the function:**
```rust
if price.yes_ask > 0 && price.no_ask > 0 { 
    let mut exposure = s.correlated_exposure.clone();
    drop(s);  // Release lock before calling
    
    check_strategies(&market.slug, &m, &mut price, capital, &mut exposure, &whale_alerts);
    
    let mut s = state.lock().await;
    s.prices.insert(market.slug.clone(), price);
    s.correlated_exposure = exposure;
}
```

2. **Make `price` mutable:**
```rust
let mut price = price_state.clone();  // Was: let price = ...
```

3. **Placeholder price detection:**
```rust
let yes_spread_pct = if p.yes_bid > 0 {
    ((p.yes_ask - p.yes_bid) as f64 / p.yes_bid as f64) * 100.0
} else { 100.0 };

if yes_spread_pct > 50.0 || no_spread_pct > 50.0 {
    // Skip placeholder prices with >50% spread
    return;
}
```

### Results

**Bot now working with real market data:**
```
📊 eth-updown-5m-1775496300 [remaining=30s] YES bid=85¢ ask=86¢ | NO bid=14¢ ask=15¢
```

- ✅ Price accumulation working
- ✅ Placeholder filtering working  
- ✅ check_strategies being called
- ✅ Real tight spreads (1¢)
- ⏳ Waiting for market conditions (need YES bid ≥90¢ for 5M HIGH-SIDE)

### Files Modified
- `src/bin/arb_bot.rs` - Added actual function call, placeholder detection

### Status
**DRY RUN ACTIVE** - Bot watching markets, waiting for conditions to trigger trades.