# Memory - March 25, 2026

## Session Update (04:53 UTC)

### Bot Status
- Bot running in DRY RUN mode (PID 27392)
- Mode switched from LIVE to DRY RUN after DEE said "Ill give you another wallet... Till then continue with dry run"
- Report cron set to every 5 minutes

### Report Script Fixed
- Problem: Report was showing old LIVE trades with "LIVE TRADING REPORT" header while in DRY RUN mode
- Fixed: Updated `/tmp/send_aws_report_inner.py` to:
  - Detect mode from `/tmp/pingpong.log`
  - Parse DRY RUN trades from log (not pnl_trades.jsonl)
  - Show "🟡 DRY RUN" header instead of "🔴 LIVE TRADING"

### Trade Tracking Issue
- DRY RUN trades are NOT being written to `pnl_trades.jsonl`
- They only appear in the log as "DRY RUN: Both legs signed & submitted!"
- Old pnl_trades.jsonl had 86 lines from previous LIVE session (all 403 errors)
- Cleared to fresh state with timestamp marker

### Current Metrics (Dry Run)
- SWEET SPOT ARBs detected: ~676 and growing
- Dry run fills logged: 17 (but not recorded to pnl_trades.jsonl)
- Report script now parses from log for dry run mode

### Pending
- DEE to provide fresh wallet private key for LIVE trading
- Wallet must be DeFi-funded (no KYC exchange history)
- USDC must be on Polygon network

## Key Identifiers (Updated)
- Bot PID: 27392
- AWS Instance: i-060737e3c67de6825
- AWS IP: 63.179.252.138
- Report Cron: */5 * * * *
- Report Script: /tmp/send_aws_report_inner.py (updated)

## Session Update (12:06 PM)

### Dry Run Results (12+ hours on AWS)
- 6,725 ARBs detected, 221 trades executed (3.3% execution rate)
- Simulated profit: $1,456 / avg $6.59/trade
- 49 unique markets traded
- Reports paused at DEE's request

### Report Script Fixes (Multiple Bugs Caught)
- Fixed profit parsing: was showing per-share, not total profit
- Fixed execution rate calculation: was using unique markets instead of total ARBs
- Both DRY and LIVE scripts now use same format

### Key Decisions Made
- Size display fixed to show actual shares (was showing orderbook depth)
- Trade complete log now shows: profit=$X.XX ($Y.YY total)
- Report format standardized for both modes

### Pending (Blocked)
- Wallet geo-block: KYC exchange origin traced - needs fresh wallet funded via DeFi only
- USDC must be on Polygon network

### DEE Asked About
- Why dry run edges (10-28%) might not transfer to LIVE
- NotebookLM query was sent but response not received before compaction


## NotebookLM Query (Not Yet Answered)
DEE asked: "Why wouldn't our strategy work in live run?"
Query sent to NotebookLM notebook `857bce48-57e6-4ee7-a621-6fe8a588a239` but response not received before compaction.
Need to retry or synthesize answer from logs/config.

## Pingpong Project Context
- AWS Frankfurt instance: i-060737e3c67de6825 (63.179.252.138)
- Bot binary: /home/ubuntu/polymarket-hft-engine/target/release/pingpong
- Bot currently PAUSED (dry run mode, cron removed)
- Report scripts: /tmp/send_aws_report_dry.py, /tmp/send_aws_report_live.py
- SSH key: ~/.ssh/pingpong.pem

## Size Configuration (main.rs edge-based sizing)
- 25%+ edge → 100 shares
- 20-25% → 75 shares  
- 15-20% → 50 shares
- 10-15% → 30 shares
- Max leg USDC: $50
- Min edge threshold: 10%

## Report Format (Standardized)
Both DRY and LIVE now use same format showing:
- ARBs Detected, Trades Executed, Execution Rate
- Total Profit, Average Profit/Trade
- Last 20 executed trades with condition ID + profit

## Session Update (16:55 EDT / 20:55 UTC)

### Polyfill-rs Integration (In Progress)
- Added `FastOrderBookProcessor` to `AppState` struct
- Added `with_fast_processor()` method to initialize SIMD parser
- polyfill-rs CrateManager and WsBookUpdateProcessor wrapped in custom struct
- Bot running with `--ws` flag (WebSocket mode, not REST polling)
- Both Primary and Backup WebSockets connected and streaming

### Current Status
- Bot running (need to check PID after restart)
- polyfill-rs infrastructure ready but NOT YET active in hot path
- Full SIMD parsing swap (using FastOrderBookProcessor in WebSocket loop) pending
- Git commits made: 976241e, 39fcf9b

### What Was Attempted
- Tried to pass `state` to `maintain_ws_connection()` for FastOrderBookProcessor access
- Initial patch had bracket/brace issues causing compile errors
- Restored working version - polyfill-rs initialized but not actively used in parsing

### Next Steps (Pending)
1. Complete polyfill-rs integration: swap `parse_orderbook_update()` to use `FastOrderBookProcessor.process()`
2. This requires passing `state` to WebSocket connection handlers
3. Or: validate current setup and move to live trading

### DEE's Last Input
- Said "Op1" meaning "Option 1: Continue optimizing (complete SIMD parsing swap)"
- Also mentioned: "Yeah" to proceeding with polyfill-rs work

### Bot Info (From Earlier Session)
- Dry run running: +$306.34 profit, 78.6% win rate, 28 slots, BTC-focused
- Config locked: 50-share min liquidity filter, 100-share fixed size
- Report cron every 10 minutes via `/tmp/send_pnl_report.sh`

### Workspace Files Modified This Session
- src/polyfill_integration.rs (new file - polyfill-rs wrapper)
- src/hot_switchover.rs (added FastOrderBookProcessor, --ws flag)
- src/main.rs (changed to use `with_fast_processor()`)
- Cargo.toml (polyfill-rs dependency already present)

## Session Update (23:35 EDT / 03:35 UTC 2026-03-26)

### polyfill-rs SIMD Integration - BLOCKED by Library Bug
**Bug confirmed in polyfill-rs 0.3.1:**
```
WsBookUpdateProcessor tape must be present
thread panicked at polyfill-rs-0.3.1/src/ws_hot_path.rs:54:14
```

- `WsBookUpdateProcessor::new()` creates processor but tape field is None
- On first `process_text()` call, `self.tape.take()` returns None → panic
- Multiple attempts to fix (Actor Pattern, direct initialization) all hit same bug
- Library bug, not our code

### What Was Done
- Attempted 3 different approaches to enable SIMD parsing
- All compiled successfully but crashed on first WebSocket message
- Reverted to stable serde_json parsing
- Committed stable state: `2e37881` (on branch main, ahead of origin by 6 commits)

### Current Bot Status
- Running on AWS: PID 118978
- Using serde_json parsing (stable, no crashes)
- 24+ SWEET SPOT ARBs detected in first 10 minutes
- Edge distribution: 17%, 23%, 24%, 25% - good range
- Primary + Backup WebSockets connected
- Dry run mode

### polyfill-rs Infrastructure Status
- ✅ `FastOrderBookProcessor` struct exists (line 801-850 in hot_switchover.rs)
- ✅ `PolyfillOrderBookState` exists in polyfill_integration.rs
- ✅ Actor Pattern implemented (local state, no Arc/Mutex contention)
- ❌ SIMD swap blocked on upstream polyfill-rs fix
- ✅ serde_json fallback working

### Options to Complete SIMD Swap
1. Wait for polyfill-rs 0.3.2+ with bug fix
2. Downgrade to older polyfill-rs version that works
3. Use simd-json crate directly instead of polyfill-rs wrapper
4. Use.rs-sIMDjson alternative

### Key Files
- Bot: /home/ubuntu/polymarket-hft-engine/target/release/pingpong
- Log: /tmp/pingpong.log
- SQLite: /tmp/pingpong.db
- Commit: 2e37881 "Stable: serde_json parsing (polyfill-rs 0.3.1 has tape initialization bug)"
