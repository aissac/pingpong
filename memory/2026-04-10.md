# Memory: 2026-04-10

## Embedding Model Benchmark (05:00-07:40 ET)

**Goal:** Improve memory recall quality across OpenClaw and MemPalace.

**Tested:**
| Model | Accuracy | Speed | Dims | Context | MTEB |
|-------|----------|-------|------|---------|------|
| nomic-embed-text (current) | **88% (7/8)** | 37ms | 768 | 8192 | 62.4 |
| qwen3-embedding:0.6b | 75% (6/8) | 124ms | 1024 | 32768 | ~60 |
| all-MiniLM-L6-v2 (MemPalace default) | ~62% | 5ms | 384 | 256 | 56 |

**Verdict:** nomic-embed-text wins. qwen3-embedding:0.6b downloaded as backup but performs worse on domain content.

**Key finding:** MemPalace's real problem was data quality (raw JSON blobs), not just the model.

## MemPalace Re-index (07:40-08:15 ET)

- Re-indexed with nomic-embed-text: 178 → 829 drawers, 384→768 dims
- Similarity scores went from negative to 0.6-0.72

## Recall Stack Correction (09:06 ET)

DEE challenged my earlier decision. Verdict:

- **MemPalace STAYS** — it provides **semantic search** on sessions, which LCM's FTS can't do. Same source data but different access pattern.
- **Graphify is NOT the last layer** — it only covers code (1439 nodes, all .rs/.py). Zero memory files.
- **Dreams system is OFF** — not writing new entries. 43 stale entries with junk tags.

**Final stack (4 layers):**
1. Memory Search — curated knowledge (semantic)
2. MemPalace — session history (semantic)
3. LCM — exact quotes, citations (FTS + DAG)
4. Graphify — code architecture (graph traversal)

**Killed:** short-term recall (broken), wiki (never built)

## Profile Monitor (AWS)
- Still running (pid 595872) as of 04:20 ET
- ClickHouse active (897MB, 171 tables, but all system tables — no user data)
- Profile monitor script fetching whale trades successfully